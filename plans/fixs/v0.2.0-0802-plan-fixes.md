# Fixes log: 0802-plan v0.2.0

- Plan: `plans/v0.2.0-0802-plan.md`
- Started: 2026-08-03T03:25:41.242602+00:00
- Reviewer: OpenAI Responses API (gpt-5.5)
- Planner: Claude
- Termination rules: severity-gated exit | NO FINDINGS | ceiling hit | planner-locked | cost-capped (v2; see plans/v2-plan.md §5.4)

## Round 1 — 2026-08-03T03:25:41.242602+00:00

### Reviewer findings (OpenAI Responses API)

1. **[HIGH]** [deployment / secret rotation] (S5 — Deploy (`python setup.py` then `gh secret set AGENT_ID ...`)) The deploy can create/archive agents successfully but leave GitHub Actions using the wrong `AGENT_ID`. The command shown does not pass the new ID value; `gh secret set AGENT_ID --repo ...` relies on interactive stdin/prompting and is easy to run with an empty, old, or mistyped value. Because the old agent is archived during setup and digest failures are explicitly unalerted, the next scheduled run can either keep producing the old prompt output or fail silently until someone checks later.
   *Concrete fix:* Make the rotation command non-ambiguous and fail-closed. Example: capture the printed ID into `NEW_AGENT_ID`, then run `gh secret set AGENT_ID --repo ZH-L1N/ai-digest-orchestrator --body "$NEW_AGENT_ID"` and require a zero exit code. Add an explicit post-rotation check step such as recording the new ID in the deployment log, confirming the setup log's recreated agent ID matches the value used for `gh secret set`, and not deploying near the scheduled run window unless the secret rotation has completed.
2. **[MEDIUM]** [test coverage gap] (S2 — Add a prompt-guard test) The proposed prompt guard can pass while several core v0.2.0 behavioral requirements are accidentally omitted or regressed. It checks headers, URLs, removed URLs, reader-action text, one exemption phrase, and length, but does not guard the layered density rules, `one event appears exactly once`, Block 3 audio compression, HN word-boundary plus `score > 50`, Reddit 429 behavior, or English-only output. A truncated or botched prompt that keeps the URLs and headers could still ship and only be noticed in the next bad digest.
   *Concrete fix:* Expand `tests/test_system_prompt.py` to assert representative exact phrases for every behavior that is contractually important: the 6–12 / 4–6 / 3–4 density ranges, `bias to FEWER`, `one event appears exactly once`, Block 3 as one closing sentence in broadcast, HN word-boundary matching and `score > 50`, Reddit 429 skip behavior, and English-only output. The test should fail if any of those sections disappear, not merely if the prompt becomes very short.
3. **[MEDIUM]** [verification gap / source coverage] (S1 `Sources to check` + S6 Verify) The plan changes the prompt to require checking 24 anchor sources, but verification never establishes that the recreated agent actually checks them. S6 only looks for headers, item count, and audio shape. If the agent ignores part of the anchor list, hits tool-call/time limits, fails on HTML-only sources, or silently skips slow/rate-limited feeds, the digest can still look structurally correct while violating the central source-floor requirement.
   *Concrete fix:* Add a deploy-time validation that inspects the actual agent run trace or logs for source access, at least for a representative dry run. If trace inspection is unavailable, add temporary/non-user-visible smoke testing before the scheduled Slack-posting run, or soften the prompt language from an unverifiable hard requirement to prioritized guidance. At minimum, update S6 to include checking the run/tool trace for coverage of the anchor categories, not just the final note shape.

### Planner decisions (Claude)

1. **Accept** (1) — Real. Bare `gh secret set` reads the value interactively and accepts an empty or mistyped paste; the old agent is already archived at that point, so a bad value leaves no working agent. Reviewer's claim that it could 'keep producing the old prompt output' is wrong (an archived agent makes sessions.create raise), but the fix stands.
   *Stated edit:* S5 rewritten: parse NEW_AGENT_ID from a tee'd setup log, guard non-empty, pass --body, add 3-point post-rotation verification, document true failure mode.
2. **Accept** (2) — Correct. The original assertion list guards structure only; a prompt keeping all headers and URLs could silently lose every behavioral rule.
   *Stated edit:* S2 split into Structure and Behavioral-contract groups; added density ranges, bias-to-FEWER, one-event-once, reader-action test, Block 3 exemption, Block 3 audio compression, HN word-boundary + score>50, Reddit 429, English-only.
3. **Accept** (3) — Gap real, primary fix not implementable in scope: run.py's event loop handles only agent.message (truncated 200 chars) and agent.custom_tool_use, so server-side web_search/web_fetch never reach the workflow log. Trace inspection needs a run.py change, explicitly out of scope. Took the reviewer's own alternative (soften unverifiable mandate) plus a cheap observable proxy.
   *Stated edit:* S1 item 2 reworded to prioritized guidance; S6 gained item 4, a hostname-frequency check over ~7 days of committed notes, labelled a smell test not a gate.

### Plan edits applied

- S5 Deploy — Fail-closed AGENT_ID rotation with --body, non-empty guard, 3-point verification, true failure mode
- S2 Prompt-guard test — Behavioral-contract assertions added alongside structural
- S1 Sources / S6 Verify — Softened source mandate; added hostname-coverage smell test

### Round stats

- Reviewer: OpenAI Responses API (gpt-5.5)
- Tokens: 4,784 input / 3,737 output
- Cost: $0.1360 (cumulative: $0.1360)
- Severity histogram: high=1, medium=2, low=0
- Duration: 0.0s
- Plan size: 9,060 chars (Δ +3,156)

### Reviewer raw response

```text
[
 {
  "severity": "high",
  "category": "deployment / secret rotation",
  "where": "S5 \u2014 Deploy (`python setup.py` then `gh secret set AGENT_ID ...`)",
  "what_can_go_wrong": "The deploy can create/archive agents successfully but leave GitHub Actions using the wrong `AGENT_ID`. The command shown does not pass the new ID value; `gh secret set AGENT_ID --repo ...` relies on interactive stdin/prompting and is easy to run with an empty, old, or mistyped value. Because the old agent is archived during setup and digest failures are explicitly unalerted, the next scheduled run can either keep producing the old prompt output or fail silently until someone checks later.",
  "concrete_fix": "Make the rotation command non-ambiguous and fail-closed. Example: capture the printed ID into `NEW_AGENT_ID`, then run `gh secret set AGENT_ID --repo ZH-L1N/ai-digest-orchestrator --body \"$NEW_AGENT_ID\"` and require a zero exit code. Add an explicit post-rotation check step such as recording the new ID in the deployment log, confirming the setup log's recreated agent ID matches the value used for `gh secret set`, and not deploying near the scheduled run window unless the secret rotation has completed."
 },
 {
  "severity": "medium",
  "category": "test coverage gap",
  "where": "S2 \u2014 Add a prompt-guard test",
  "what_can_go_wrong": "The proposed prompt guard can pass while several core v0.2.0 behavioral requirements are accidentally omitted or regressed. It checks headers, URLs, removed URLs, reader-action text, one exemption phrase, and length, but does not guard the layered density rules, `one event appears exactly once`, Block 3 audio compression, HN word-boundary plus `score > 50`, Reddit 429 behavior, or English-only output. A truncated or botched prompt that keeps the URLs and headers could still ship and only be noticed in the next bad digest.",
  "concrete_fix": "Expand `tests/test_system_prompt.py` to assert representative exact phrases for every behavior that is contractually important: the 6\u201312 / 4\u20136 / 3\u20134 density ranges, `bias to FEWER`, `one event appears exactly once`, Block 3 as one closing sentence in broadcast, HN word-boundary matching and `score > 50`, Reddit 429 skip behavior, and English-only output. The test should fail if any of those sections disappear, not merely if the prompt becomes very short."
 },
 {
  "severity": "medium",
  "category": "verification gap / source coverage",
  "where": "S1 `Sources to check` + S6 Verify",
  "what_can_go_wrong": "The plan changes the prompt to require checking 24 anchor sources, but verification never establishes that the recreated agent actually checks them. S6 only looks for headers, item count, and audio shape. If the agent ignores part of the anchor list, hits tool-call/time limits, fails on HTML-only sources, or silently skips slow/rate-limited feeds, the digest can still look structurally correct while violating the central source-floor requirement.",
  "concrete_fix": "Add a deploy-time validation that inspects the actual agent run trace or logs for source access, at least for a representative dry run. If trace inspection is unavailable, add temporary/non-user-visible smoke testing before the scheduled Slack-posting run, or soften the prompt language from an unverifiable hard requirement to prioritized guidance. At minimum, update S6 to include checking the run/tool trace for coverage of the anchor categories, not just the final note shape."
 }
]
```

## Round 2 — 2026-08-03T03:27:00.877664+00:00

### Reviewer findings (OpenAI Responses API)

1. **[HIGH]** [deployment / fail-closed rotation] (S5 deploy command: `python setup.py | tee /tmp/setup-v0.2.0.log` followed by parsing and rotating `AGENT_ID`) The revised rotation is still not fail-closed because the pipeline masks `setup.py`'s exit status. In POSIX shells without `set -o pipefail`, `python setup.py | tee ...` returns `tee`'s status, not Python's. If `setup.py` creates or prints an `AGENT_ID` and then exits non-zero due to a later failure, the plan can still parse that ID and update the GitHub secret. The next scheduled run can then point at an agent/environment setup that did not complete cleanly, producing a red workflow with no alerting unless someone checks manually. This undermines the accepted prior fix's stated goal of fail-closed rotation.
   *Concrete fix:* Make the setup invocation explicitly check Python's exit status before parsing or rotating the secret. For example: `set -euo pipefail` before the block, or `python setup.py > /tmp/setup-v0.2.0.log 2>&1; SETUP_RC=$?; cat /tmp/setup-v0.2.0.log; test "$SETUP_RC" -eq 0 || { echo "FATAL: setup.py failed"; exit "$SETUP_RC"; }`. Only then parse `NEW_AGENT_ID` and call `gh secret set --body`. Keep the existing non-empty and log-match checks.

### Planner decisions (Claude)

1. **Accept** (1) — Correct and important. `python setup.py | tee log` reports tee's exit status, so a setup.py that printed an AGENT_ID and then failed later would still pass the guard and get a half-finished ID promoted into the secret — defeating the round-1 fix. Additionally, the reviewer's own suggested remedy is bash-only; the repo owner's default shell is fish, where `set -o pipefail` and `$?` are syntax errors, so the block needed an explicit bash designation the finding did not mention.
   *Stated edit:* S5 command block rewritten: redirect with `>` instead of piping to tee, capture `$?` into SETUP_RC on the same line (required under `set -e`), gate rotation on SETUP_RC==0, and label the block bash-only with a fish warning.

### Plan edits applied

- S5 Deploy — Exit-status-safe setup invocation; bash-only designation with fish warning

### Round stats

- Reviewer: OpenAI Responses API (gpt-5.5)
- Tokens: 9,059 input / 1,388 output
- Cost: $0.0869 (cumulative: $0.2230)
- Severity histogram: high=1, medium=0, low=0
- Duration: 0.0s
- Plan size: 9,811 chars (Δ +751)

### Reviewer raw response

```text
{"status":"FINDINGS_PRESENT","findings":[{"severity":"high","category":"deployment / fail-closed rotation","where":"S5 deploy command: `python setup.py | tee /tmp/setup-v0.2.0.log` followed by parsing and rotating `AGENT_ID`","what_can_go_wrong":"The revised rotation is still not fail-closed because the pipeline masks `setup.py`'s exit status. In POSIX shells without `set -o pipefail`, `python setup.py | tee ...` returns `tee`'s status, not Python's. If `setup.py` creates or prints an `AGENT_ID` and then exits non-zero due to a later failure, the plan can still parse that ID and update the GitHub secret. The next scheduled run can then point at an agent/environment setup that did not complete cleanly, producing a red workflow with no alerting unless someone checks manually. This undermines the accepted prior fix's stated goal of fail-closed rotation.","concrete_fix":"Make the setup invocation explicitly check Python's exit status before parsing or rotating the secret. For example: `set -euo pipefail` before the block, or `python setup.py > /tmp/setup-v0.2.0.log 2>&1; SETUP_RC=$?; cat /tmp/setup-v0.2.0.log; test \"$SETUP_RC\" -eq 0 || { echo \"FATAL: setup.py failed\"; exit \"$SETUP_RC\"; }`. Only then parse `NEW_AGENT_ID` and call `gh secret set --body`. Keep the existing non-empty and log-match checks."}],"open_questions":[]}
```

## Round 3 — 2026-08-03T03:28:22.708732+00:00

### Reviewer findings (OpenAI Responses API)

1. **[HIGH]** [deployment script / errexit handling] (S5 Deploy bash block: `set -euo pipefail` followed by `python setup.py > /tmp/setup-v0.2.0.log 2>&1; SETUP_RC=$?`) The prior finding is not actually fixed as stated. In bash with `set -e` active, if `python setup.py` exits non-zero, bash exits immediately; the `SETUP_RC=$?`, `cat`, and explicit `FATAL: ... not rotating` check do not run. If `setup.py` has already archived the old agent before failing, the operator can be left with the GitHub secret still pointing at an archived agent and no graceful recovery/diagnostic path from the planned block; the next scheduled workflow goes red and remains unalerted unless someone checks manually.
   *Concrete fix:* Do not rely on `; SETUP_RC=$?` under `errexit`. Use an `if` or temporarily disable errexit around the setup invocation, e.g.:

```bash
set -euo pipefail
export ANTHROPIC_API_KEY=sk-ant-...

if python setup.py > /tmp/setup-v0.2.0.log 2>&1; then
  SETUP_RC=0
else
  SETUP_RC=$?
fi
cat /tmp/setup-v0.2.0.log
test "$SETUP_RC" -eq 0 || { echo "FATAL: setup.py exited $SETUP_RC — not rotating"; exit "$SETUP_RC"; }
```

Then parse and rotate only after this gate passes. Remove the incorrect note claiming same-line `$?` capture avoids `set -e` aborts.

### Planner decisions (Claude)

1. **Accept** (1) — Reviewer is right and the round-2 edit was wrong. Under `set -e`, `cmd; VAR=$?` does NOT suppress errexit — bash aborts on cmd's non-zero exit before the capture, cat, and FATAL check execute. Only if/||/&&/! contexts protect. The explanatory note I added in round 2 asserted the opposite and was incorrect.
   *Stated edit:* S5 block rewritten with an `if python setup.py ...; then SETUP_RC=0; else SETUP_RC=$?; fi` wrapper; the incorrect same-line-capture note removed and replaced with an accurate explanation of which contexts suppress errexit and why the diagnostic path matters here.

### Plan edits applied

- S5 Deploy — if-wrapper for errexit-safe exit capture; removed incorrect same-line `$?` claim

### Round stats

- Reviewer: OpenAI Responses API (gpt-5.5)
- Tokens: 8,269 input / 2,227 output
- Cost: $0.1082 (cumulative: $0.3312)
- Severity histogram: high=1, medium=0, low=0
- Duration: 0.0s
- Plan size: 10,135 chars (Δ +324)

### Reviewer raw response

```text
{"status":"FINDINGS_PRESENT","findings":[{"severity":"high","category":"deployment script / errexit handling","where":"S5 Deploy bash block: `set -euo pipefail` followed by `python setup.py > /tmp/setup-v0.2.0.log 2>&1; SETUP_RC=$?`","what_can_go_wrong":"The prior finding is not actually fixed as stated. In bash with `set -e` active, if `python setup.py` exits non-zero, bash exits immediately; the `SETUP_RC=$?`, `cat`, and explicit `FATAL: ... not rotating` check do not run. If `setup.py` has already archived the old agent before failing, the operator can be left with the GitHub secret still pointing at an archived agent and no graceful recovery/diagnostic path from the planned block; the next scheduled workflow goes red and remains unalerted unless someone checks manually.","concrete_fix":"Do not rely on `; SETUP_RC=$?` under `errexit`. Use an `if` or temporarily disable errexit around the setup invocation, e.g.:\n\n```bash\nset -euo pipefail\nexport ANTHROPIC_API_KEY=sk-ant-...\n\nif python setup.py > /tmp/setup-v0.2.0.log 2>&1; then\n  SETUP_RC=0\nelse\n  SETUP_RC=$?\nfi\ncat /tmp/setup-v0.2.0.log\ntest \"$SETUP_RC\" -eq 0 || { echo \"FATAL: setup.py exited $SETUP_RC — not rotating\"; exit \"$SETUP_RC\"; }\n```\n\nThen parse and rotate only after this gate passes. Remove the incorrect note claiming same-line `$?` capture avoids `set -e` aborts."}],"open_questions":[]}
```

## Round 4 — 2026-08-03T03:30:17.968759+00:00

### Reviewer findings (OpenAI Responses API)

1. **[HIGH]** [deployment / destructive failure handling] (S5 deploy block and new explanation after the bash snippet) The prior `errexit` bug is fixed, but the failure path is still operationally unsafe: if `setup.py` archives the old agent and then exits non-zero, the script now correctly refuses to rotate `AGENT_ID`, leaving GitHub Actions pointing at an archived agent. The next scheduled digest fails until someone manually recovers, and failure alerting is explicitly absent. The new diff even states this is the exact state the operator needs reported, but the plan only reports it and exits; it does not tell the operator how to restore service or prevent the next scheduled red run.
   *Concrete fix:* Add an explicit recovery branch/checklist for `SETUP_RC != 0` before the script exits. At minimum: say the deploy is now in a broken state if the log shows the old agent was archived; require immediate recovery before leaving the terminal; either revert to the previous prompt and re-run `setup.py` to create a replacement old-prompt agent and rotate `AGENT_ID`, or fix the setup failure and re-run successfully; if recovery cannot be completed before the next schedule, disable the workflow or otherwise prevent an unobserved failed run. Stronger fix: change `setup.py`/`ensure_agent` to create and verify the replacement agent before archiving the old one, so setup failure does not invalidate the currently configured `AGENT_ID`.

### Planner decisions (Claude)

1. **Accept** (1) — Gap is real and verified: ensure_agent archives at setup.py:573 then creates at :578, neither guarded by the other, so a create failure strands the AGENT_ID secret on an archived agent with no alerting. Accepted the recovery-checklist half of the fix. Rejected the 'create-before-archive refactor' half: re-running setup.py already recovers in one command (find_matching returns nothing once archived, so it creates fresh), making this a documentation gap rather than a code gap; rewriting the deploy path to guard a low-probability event adds risk to the very path being hardened.
   *Stated edit:* S5 gained an 'If SETUP_RC != 0' recovery subsection: how to identify the broken state from the log, re-run as the primary fix, and `gh workflow disable` as the fallback when recovery cannot finish before the next schedule. The rejected refactor is recorded in Out of scope with its reasoning.

### Plan edits applied

- S5 Deploy — Added mid-deploy failure recovery checklist (identify, re-run, disable-workflow fallback)
- Out of scope — Recorded create-before-archive refactor as deliberately declined, with reasoning

### Round stats

- Reviewer: OpenAI Responses API (gpt-5.5)
- Tokens: 8,693 input / 2,406 output
- Cost: $0.1156 (cumulative: $0.4468)
- Severity histogram: high=1, medium=0, low=0
- Duration: 0.0s
- Plan size: 11,585 chars (Δ +1,450)

### Reviewer raw response

```text
{"status":"FINDINGS_PRESENT","findings":[{"severity":"high","category":"deployment / destructive failure handling","where":"S5 deploy block and new explanation after the bash snippet","what_can_go_wrong":"The prior `errexit` bug is fixed, but the failure path is still operationally unsafe: if `setup.py` archives the old agent and then exits non-zero, the script now correctly refuses to rotate `AGENT_ID`, leaving GitHub Actions pointing at an archived agent. The next scheduled digest fails until someone manually recovers, and failure alerting is explicitly absent. The new diff even states this is the exact state the operator needs reported, but the plan only reports it and exits; it does not tell the operator how to restore service or prevent the next scheduled red run.","concrete_fix":"Add an explicit recovery branch/checklist for `SETUP_RC != 0` before the script exits. At minimum: say the deploy is now in a broken state if the log shows the old agent was archived; require immediate recovery before leaving the terminal; either revert to the previous prompt and re-run `setup.py` to create a replacement old-prompt agent and rotate `AGENT_ID`, or fix the setup failure and re-run successfully; if recovery cannot be completed before the next schedule, disable the workflow or otherwise prevent an unobserved failed run. Stronger fix: change `setup.py`/`ensure_agent` to create and verify the replacement agent before archiving the old one, so setup failure does not invalidate the currently configured `AGENT_ID`."}],"open_questions":[]}
```

## Round 5 — 2026-08-03T03:59:04.892181+00:00

### Reviewer findings (OpenAI Responses API)

1. **[HIGH]** [deployment / failure recovery] (S5 new subsection: `If SETUP_RC != 0 — you are mid-deploy and service is down`, especially recovery step 3) A harmless failed deployment attempt can be turned into a real production outage. The new guidance treats any non-zero `setup.py` exit as if the old agent has already been archived and tells the operator to disable the scheduled workflow if setup keeps failing. But `setup.py` can fail before archiving the currently configured agent, for example due to local environment/dependency issues, an invalid local `ANTHROPIC_API_KEY`, CLI/network problems before the archive call, or another preflight failure. In those cases the GitHub secret still points at the old working agent, and the next scheduled run would continue producing the old-format digest. Following the plan would instead disable `daily-digest.yml` and guarantee a missed digest.
   *Concrete fix:* Split the recovery path based on whether the log proves the old agent was archived. Add an explicit safe-no-op branch: if `/tmp/setup-v0.2.0.log` does not show the archive/recreate point for the currently configured `AGENT_ID`, do not disable the workflow and do not rotate anything; leave the old workflow running and retry deployment later. Only use `gh workflow disable ...` when the log confirms the old configured agent was archived or the secret was already rotated to a bad/new unusable ID. The heading should not say all `SETUP_RC != 0` means service is down; make it conditional on the archive evidence.

### Planner decisions (Claude)

1. **Accept** (1) — Correct and the most consequential finding of the run: the round-4 recovery text would convert a harmless pre-archive failure into a guaranteed missed digest by disabling the workflow while the old agent and AGENT_ID secret were still valid and serving.
   *Stated edit:* S5 recovery restructured into a grep-first gate with Branch A (safe no-op) and Branch B (service down).

### Plan edits applied

- S5 Deploy / recovery — Grep-first two-branch recovery; safe-no-op path added

### Deferrals at exit

- **[HIGH]** (r5-edit-unvalidated) → accepted-at-exit: Round-5 edit to the S5 recovery procedure was never re-reviewed; ceiling hit at 5 rounds and user accepted the risk at exit. Context: rounds 3, 4 and 5 each found a real defect in the prior round's fix to this same block, so the base rate for a further defect is non-trivial. Mitigating: S5 is a manual operator step, read before execution, and the round-5 change only added a safe no-op branch.

### Round stats

- Reviewer: OpenAI Responses API (gpt-5.5)
- Tokens: 8,972 input / 1,299 output
- Cost: $0.0838 (cumulative: $0.5306)
- Severity histogram: high=1, medium=0, low=0
- Duration: 0.0s
- Plan size: 12,103 chars (Δ +518)

### Reviewer raw response

```text
{"status":"FINDINGS_PRESENT","findings":[{"severity":"high","category":"deployment / failure recovery","where":"S5 new subsection: `If SETUP_RC != 0 — you are mid-deploy and service is down`, especially recovery step 3","what_can_go_wrong":"A harmless failed deployment attempt can be turned into a real production outage. The new guidance treats any non-zero `setup.py` exit as if the old agent has already been archived and tells the operator to disable the scheduled workflow if setup keeps failing. But `setup.py` can fail before archiving the currently configured agent, for example due to local environment/dependency issues, an invalid local `ANTHROPIC_API_KEY`, CLI/network problems before the archive call, or another preflight failure. In those cases the GitHub secret still points at the old working agent, and the next scheduled run would continue producing the old-format digest. Following the plan would instead disable `daily-digest.yml` and guarantee a missed digest.","concrete_fix":"Split the recovery path based on whether the log proves the old agent was archived. Add an explicit safe-no-op branch: if `/tmp/setup-v0.2.0.log` does not show the archive/recreate point for the currently configured `AGENT_ID`, do not disable the workflow and do not rotate anything; leave the old workflow running and retry deployment later. Only use `gh workflow disable ...` when the log confirms the old configured agent was archived or the secret was already rotated to a bad/new unusable ID. The heading should not say all `SETUP_RC != 0` means service is down; make it conditional on the archive evidence."}],"open_questions":[]}
```
